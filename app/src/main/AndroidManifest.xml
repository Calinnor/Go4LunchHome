<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cirederf.go4lunch">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <uses-feature
        android:name="android.hardware.location"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Go4Lunch">
        <activity android:name=".views.activities.TryDetails"></activity>
        <activity android:name=".views.activities.FetchRestaurantDetailsActivity" />
        <activity android:name=".views.activities.DetailsRestaurantActivity" />
        <activity android:name=".views.activities.BaseActivity" />
        <activity android:name=".views.activities.LoginActivity" />
        <activity android:name=".views.activities.StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".views.activities.MainActivity" />
    </application>

</manifest>